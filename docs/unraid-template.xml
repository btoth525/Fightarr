<?xml version="1.0"?>
<Container version="2">
  <Name>Fightarr</Name>
  <Repository>ghcr.io/btoth525/fightarr:latest</Repository>
  <Registry>https://github.com/btoth525/Fightarr/pkgs/container/fightarr</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/btoth525/Fightarr/issues</Support>
  <Project>https://github.com/btoth525/Fightarr</Project>
  <Overview>UFC event manager for Usenet and Plex. Automatically searches, grabs, renames, and imports UFC fight cards -- the Radarr for UFC that Radarr refuses to be.

IMPORTANT -- PATH SETUP (read before installing):

Fightarr needs to see two things: where SABnzbd puts completed downloads, and where your media library lives. The defaults below match the most common Unraid layout:

  /mnt/user/downloads  ->  /downloads   (SAB drops finished files here)
  /mnt/user/media      ->  /media       (your media library root)

After install, go to Fightarr Settings -&gt; Media Management and set Root Folder to /media (or a subfolder like /media/UFC if you want UFC files in their own folder).

In SABnzbd Settings -&gt; Categories, create a category named "ufc" and set its folder to the path inside /downloads where SAB puts that category (e.g. /downloads/complete/ufc). Fightarr picks up completed downloads from that path automatically.

By default Fightarr MOVES the file out of /downloads into /media (instant rename on the same filesystem, zero extra disk space). If you want to keep the original in downloads for seeding, enable "Use Hardlinks" in Fightarr Settings -&gt; Media Management -- hardlinks only work when /downloads and /media are on the same Unraid array/pool. Permissions are set automatically (0775 folder, 0664 file) to match Unraid nobody:users + UMASK 002.

TRaSH-Guides /data layout: If you prefer a single unified mount, change the Downloads path to /mnt/user/data -&gt; /data, mirror that in SABnzbd, and set Root Folder to /data/media/ufc. See the Fightarr README Unraid section for the full walkthrough.</Overview>
  <Category>MediaApp:Video Tools:</Category>
  <WebUI>http://[IP]:[PORT:7879]/</WebUI>
  <TemplateURL/>
  <Icon>https://raw.githubusercontent.com/btoth525/Fightarr/main/frontend/public/logo.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>

  <!-- Port -->
  <Config Name="WebUI Port" Target="7878" Default="7879" Mode="tcp"
    Description="Web UI and API port. Default 7879 avoids conflict with Radarr (7878). Access Fightarr at http://[IP]:[PORT:7879]/"
    Type="Port" Display="always" Required="true" Mask="false">7879</Config>

  <!-- Paths -->
  <Config Name="Config" Target="/config" Default="/mnt/user/appdata/fightarr" Mode="rw"
    Description="Stores the SQLite database (fightarr.db), all settings, and API keys. Never share this folder with another container."
    Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/fightarr</Config>

  <Config Name="Downloads" Target="/downloads" Default="/mnt/user/downloads" Mode="rw"
    Description="Where SABnzbd (or your other download client) saves completed downloads. Must be the same physical share that SAB writes to -- Fightarr reads completed files from here before moving them to Plex. Example: /mnt/user/downloads"
    Type="Path" Display="always" Required="true" Mask="false">/mnt/user/downloads</Config>

  <Config Name="Media" Target="/media" Default="/mnt/user/media" Mode="rw"
    Description="Root of your media library. Fightarr renames and moves imported UFC files here. Must be the same share your media player (Plex/Jellyfin/Emby) reads from. Example: /mnt/user/media"
    Type="Path" Display="always" Required="true" Mask="false">/mnt/user/media</Config>

  <!-- Identity -->
  <Config Name="PUID" Target="PUID" Default="99" Mode=""
    Description="Run-as user ID. 99 = nobody on Unraid. Must match the owner UID of your Downloads and Plex shares so Fightarr can read and write files."
    Type="Variable" Display="always" Required="true" Mask="false">99</Config>

  <Config Name="PGID" Target="PGID" Default="100" Mode=""
    Description="Run-as group ID. 100 = users on Unraid."
    Type="Variable" Display="always" Required="true" Mask="false">100</Config>

  <Config Name="UMASK" Target="UMASK" Default="002" Mode=""
    Description="File permission mask. 002 = group-writable, matches Radarr/Sonarr defaults."
    Type="Variable" Display="always" Required="false" Mask="false">002</Config>

  <Config Name="TZ" Target="TZ" Default="America/New_York" Mode=""
    Description="Timezone (e.g. America/New_York, America/Chicago, America/Los_Angeles, Europe/London). Affects scheduled task timing and log timestamps."
    Type="Variable" Display="always" Required="true" Mask="false">America/New_York</Config>

  <!-- Logging -->
  <Config Name="Log Level" Target="FIGHTARR_LOG_LEVEL" Default="INFO" Mode=""
    Description="Logging verbosity: DEBUG, INFO, WARNING, ERROR. Use DEBUG only when troubleshooting."
    Type="Variable" Display="always" Required="false" Mask="false">INFO</Config>

  <!-- Optional integrations -->
  <Config Name="TMDB API Key" Target="FIGHTARR_TMDB_API_KEY" Default="" Mode=""
    Description="Optional. Better poster art from TMDB. Poster art works automatically via Wikipedia without this key."
    Type="Variable" Display="always" Required="false" Mask="true"/>

  <Config Name="Plex Host" Target="FIGHTARR_PLEX_HOST" Default="" Mode=""
    Description="Optional. Plex server URL for automatic library refresh after import. Example: http://192.168.1.10:32400. Can also be set in Fightarr Settings -&gt; Connect."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="Plex Token" Target="FIGHTARR_PLEX_TOKEN" Default="" Mode=""
    Description="Optional. Plex authentication token (X-Plex-Token). Find it in Plex Web: click your avatar -> Account -> find token in any API URL."
    Type="Variable" Display="advanced" Required="false" Mask="true"/>

  <Config Name="Plex Section ID" Target="FIGHTARR_PLEX_SECTION_ID" Default="" Mode=""
    Description="Optional. Plex library section ID to refresh after import. Leave blank to refresh all sections. Find the ID in Plex Web under your library URL."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="Jellyfin Host" Target="FIGHTARR_JELLYFIN_HOST" Default="" Mode=""
    Description="Optional. Jellyfin server URL for library refresh after import. Example: http://192.168.1.10:8096"
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="Jellyfin Token" Target="FIGHTARR_JELLYFIN_TOKEN" Default="" Mode=""
    Description="Optional. Jellyfin API key."
    Type="Variable" Display="advanced" Required="false" Mask="true"/>

  <!-- Advanced -->
  <Config Name="CORS Origins" Target="FIGHTARR_CORS_ORIGINS" Default="*" Mode=""
    Description="Comma-separated allowed CORS origins, or * to allow all. * is fine for LAN-only. Tighten if reverse-proxying to the internet."
    Type="Variable" Display="advanced" Required="false" Mask="false">*</Config>

  <Config Name="Schedule Refresh Interval" Target="FIGHTARR_SCHEDULE_REFRESH_INTERVAL" Default="21600" Mode=""
    Description="How often (seconds) to sync the UFC schedule from Wikipedia. Default: 21600 (6 hours)."
    Type="Variable" Display="advanced" Required="false" Mask="false">21600</Config>

  <Config Name="Indexer Search Interval" Target="FIGHTARR_INDEXER_SEARCH_INTERVAL" Default="1800" Mode=""
    Description="How often (seconds) to RSS-search indexers for monitored missing events. Default: 1800 (30 minutes)."
    Type="Variable" Display="advanced" Required="false" Mask="false">1800</Config>

</Container>
